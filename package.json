{
  "name": "tom-bot",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "snowball",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "start": "pm2 restart app.js --name tom-bot --watch && pm2 logs tom-bot",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "discord.js": "^14.25.1",
    "dotenv": "^17.2.3",
    "mongodb": "^7.0.0"
  }
}
